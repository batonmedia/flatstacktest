$(document).ready(function () {
    var headerNav = $('.header-nav');

    //$(window)
    //    .on("scroll", function () {
    //        $(window).scrollTop() > 40 ? headerNav.addClass('header-nav-fixed') : headerNav.removeClass('header-nav-fixed');
    //    })
    //    .trigger("scroll");
});